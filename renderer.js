// const { ipcRenderer } = require('electron');

// ipcRenderer.on('TEST-1', (event, message) => {
//   // this works
//   console.log(message);
//   ipcRenderer.send('TEST-2', 'message sent from renderer');
// });

// console.log(window.ipcRenderer);
